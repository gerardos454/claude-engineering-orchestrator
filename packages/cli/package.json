{
  "name": "@engineer/cli",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=24" },
  "type": "module",
  "bin": { "engineer": "./dist/bin.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import=tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@engineer/pack-sdk": "0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
